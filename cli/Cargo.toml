[package]
name = "sudokube-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
sudokube-core = { path = "../core" }
rand = "0.8"
crossterm = "0.28"
chrono = "0.4"
rusqlite = { version = "0.34", features = ["bundled"] }
