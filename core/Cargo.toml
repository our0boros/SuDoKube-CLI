[package]
name = "sudokube-core"
version = "0.1.0"
edition = "2024"

[features]
default = []
bevy = ["dep:bevy"]

[dependencies]
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
bevy = { version = "0.18.1", optional = true }
